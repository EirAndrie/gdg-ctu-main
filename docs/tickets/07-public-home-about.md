# 07: Public Home + About feeds

Blocked by: 06

## What to build
- Home order: hero CMS override -> Team carousel (?featured max 8-10) -> Recent Events (upcoming-first 3 backfill) -> Partners strip (all active tier-ordered) -> Captured Moments (/featured max 8-10) -> cta/footer.
- About: /public/content about+community override with hardcoded fallback.
- Nav: Officers->Our Team (/team), +Partners, /officers redirect -> /team.

## Acceptance
- Each strip links to full page, fallback when CMS empty.
- Spec: docs/admin-cms-spec.md S1 Home R5 locked.

