# 06: Public Events + Team + Partners + Gallery

Blocked by: 01, 02

## What to build
- Events page tabs Upcoming/Featured/Past via ?scope + /events/:slug detail.
- Officers -> /team from public team API, grouped dept (Exec first).
- Partners page grouped by tier.
- Gallery album grid + /gallery/:slug detail with ordered items.
- Skeleton/empty/error+retry, hide-if-empty feeds.

## Acceptance
- Published/active-only renders, 404 slug -> not-published state.
- Spec: docs/admin-cms-spec.md S5, S10.3.

